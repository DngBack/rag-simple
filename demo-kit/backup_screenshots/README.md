# Backup screenshots & transcripts

Đặt tại đây:
- **Ảnh chụp màn hình** output khi chạy demo (phòng khi mạng/latency trên lớp).
- **Transcript** copy từ output hoặc từ file trong `../transcripts/` (backup_demo1_*, backup_demo3_*).

Quy tắc: khi live không chạy hoặc model “quá đúng”, chiếu backup thay vì sửa tay output.
